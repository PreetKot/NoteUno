{
  "name": "noteuno",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "npm run start --prefix backend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "vercel-build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend"
  },
  "keywords": ["note-taking", "mern", "react", "nodejs", "mongodb"],
  "author": "Preet Kot",
  "license": "ISC",
  "description": "A modern note-taking application with authentication and file attachments",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
